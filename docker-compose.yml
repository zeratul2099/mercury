version: '2.0'
services:
    mercury:
        build: .
        image: "${MERCURY_IMAGE}"
        ports:
        - "5001:5001"
        volumes:
        - ./settings.yaml:/home/rust/mercury/settings.yaml
